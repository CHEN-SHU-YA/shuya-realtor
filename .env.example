# 複製成 .env.local（本機）或填到部署平台的環境變數。
# 這個檔案本身不要放真實密碼，它會被提交進 Git。

# --- 後台存取控制（正式環境必填）---------------------------
# /admin/appointments 會顯示客戶姓名、電話、Email 與需求內容。
# 正式環境沒設定這兩個變數，後台會直接停用（回 503），不會裸奔。
ADMIN_USER=
ADMIN_PASS=

# --- 網站對外網址（選填）-----------------------------------
# 影響 canonical、Open Graph 圖片與結構化資料。
# 沒填會自動用 Vercel 給的網址；接自訂網域後再填正式網域。
NEXT_PUBLIC_SITE_URL=

# --- 資料庫（正式環境必填）---------------------------------
# 預約單存哪裡：
#   有填 DATABASE_URL → 存 Postgres（正式站一定要走這條）
#   沒填              → 退回寫 data/appointments.json（本機開發用）
#
# Vercel 的檔案系統是唯讀的，部署後沒有資料庫就收不到任何一筆預約 ——
# 客戶按了送出會看到「請直接打電話給我」，然後就走了。
#
# 線上端：在 Vercel 專案 Storage 連上 Postgres 之後會自動注入，不用手動填。
# 本機端：到 Vercel → Storage → shuya-db → 複製連線字串貼到 .env.local。
DATABASE_URL=
